<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
  </style>

  <!-- no action bar -->
  <style name="AppTheme.NoActionBar" parent="AppTheme">
    <item name="windowActionBar">false</item>
    <item name="windowNoTitle">true</item>
  </style>

  <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

  <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

  <!-- loading progress indicator -->
    <style name="AppTheme.ProgressBar"><item name="colorAccent">@color/colorPrimary</item></style>

    <style name="AppTheme.SearchView">
        <!--<item name="colorControlNormal">@color/colorPrimary</item>-->
        <item name="colorControlActivated">@color/colorPrimary</item> // change this color to the required cursor color your need.
        <!--<item name="colorControlHighlight">@color/colorPrimary</item>-->
    </style>

</resources>
